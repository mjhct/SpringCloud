server:
  port: 83
  servlet:
    context-path: /consumer
spring:
  application:
    name: nacos-consumer-service
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848
        file-extension: yaml
        group: TEST_GROUP
server-url:
  provider-service: http://nacos-provider-service